// @title Competitor Website Intelligence Tracker With Long Market Notes Flow 2
// @description Capture competitor pages, extract pricing and positioning changes, compare messaging over time, and generate long-form market intelligence notes. This variant intentionally has a longer description so the store detail view can be tested with wrapping text, multiple rows, and dense content layouts.
async function run($page, $input) {
  return $generateResponseSuccess('Competitor Website Intelligence Tracker With Long Market Notes Flow 2 completed', {
    namespace: 'demo_competitor',
    variant: 2,
    input: $input,
  });
}
