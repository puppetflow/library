// @title HubSpot CRM Data Enrichment Suite Snippet 3
// @description Builds a normalized payload for HubSpot CRM Data Enrichment Suite. This deliberately longer helper description checks truncation, spacing, and readability inside dense blueprint detail lists.
// @param $input Input payload passed by the caller.
return {
  namespace: 'demo_hubspot',
  variant: 3,
  input: $input,
};
